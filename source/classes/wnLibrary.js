/*@
 * Classe: wnLibrary
 * Desc: Estrutura padrão de uma biblioteca.
 */

// Exportar:
module.exports = wnLibrary;

// Construtor:
function wnLibrary(file) {

	// Herança:
	util.inherits(this, wnApp);

}