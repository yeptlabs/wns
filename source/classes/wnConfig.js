/*@
 * Classe: wnConfig
 * Desc: Estrutura padrão de uma configuração.
 */

// Exportar:
module.exports = wnConfig;

// Construtor:
function wnConfig(file) {

	// Herança:
	util.inherits(this, wnObject);

}