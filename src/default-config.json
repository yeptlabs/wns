// Server configuration
{
	// Define app directory (uncomment to run applications)
	// "appDirectory": "apps/",
	
	// Components
	"components": {

		// HTTP server configuration
		// Requires `wns-http-package`, install it typing `wnspm install http`
		// And uncomment lines below to activate.
		// "http": {
			// "class": "wnHttp",
			// "listen":[80]
		// }

	}
}